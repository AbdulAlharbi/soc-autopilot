# Optional: enables the Claude reasoning back-end. Without it the deterministic heuristic brain is used.
ANTHROPIC_API_KEY=
# claude | heuristic | auto
SOCPILOT_BRAIN=auto
SOCPILOT_MODEL=claude-sonnet-4-5
# Business timezone offset from UTC for off-hours detection (Riyadh = 3)
SOCPILOT_TZ_OFFSET=3
